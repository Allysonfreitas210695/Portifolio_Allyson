import styled from 'styled-components';

export const WrapperSkills = styled.div`
  flex: 1;
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 15px;
  overflow: hidden;
`;

export const WrappeSkill = styled.div`
  width: 480px;
  padding: 10px;
`;

export const ContainerSkill = styled.div`
  height: auto;
  display: flex;
  flex-wrap: wrap;
  justify-content: start;
  margin-right: 15px;
`;